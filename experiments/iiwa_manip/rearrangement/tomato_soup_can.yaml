# Unique identifier for this experiment for logging
experiment_id: "iiwa_manip_rearrangement_tomato_soup_can"

env:
  contact_model: "hydroelastic_with_fallback"
  solver: "sap"
  obj_name: "ycb_tomato_soup_can"

script:
  name: "iiwa_manip_tomato_soup_can"
  args:
    sim_duration: -1.0 # Not used for IIWARearrangementSimulator
    timestep: 0.005
    logging_frequency_hz: 100
    hydroelastic_manipuland: true

# Component specifications
# 'args' are constructor arguments that are not part of the base class constructors
logger:
  class: "DynamicLogger"
  args:
    label_to_mask: 22 # This label corresponds to the tomato soup can in the iiwa_manip_tomato_soup_can setup
    manipuland_name: "ycb_tomato_soup_can"
    manipuland_base_link_name: "ycb_tomato_soup_can_base_link"
    kProximity: false

# image_generator:
#   class: "IIWAWristSphereImageGenerator"
#   args:
#     simulate_time: 0.1
#     look_at_point: [0.0, 0.5, 0.050450]
#     z_distances: [0.3, 0.4]
#     radii: [0.3, 0.25]
#     num_poses: [20, 20]
#     time_between_camera_waypoints: 1.0
#     has_leg_camera: True
#     num_cameras_below_table: 5

# image_generator:
#   class: "NoneImageGenerator"
#   args:
#     simulate_time: 0.1
#     look_at_point: [0.0, 0.5, 0.050450]
#     z_distances: [0.0, 0.06, 0.12, -0.16, -0.20]
#     radii: [0.25, 0.2, 0.15, 0.2, 0.15]
#     num_poses: [30, 25, 15, 25, 15]
#     z_distances: [0.0]
#     radii: [0.25]
#     num_poses: [1]

image_generator:
  class: "NoneImageGenerator"
  args:

inverse_graphics:
  class: "IdentityInverseGraphics"
  args:
    mesh_path: ./data/tomato_soup_can_instant_ngp.obj
    mesh_pose: [0.0, 0.0, 0.0, 0.0, 0.5, 0.050450] # NOTE: Identical to `X_WManipuland` specified in `table_pid.py`

mesh_processor:
  class: "QuadricDecimationMeshProcessor"
  args:
    target_triangle_num: 5000

physical_property_estimator:
  class: "WaterDensityPhysicalPropertyEstimator"
  args:

simulator:
  class: "IIWARearrangementSimulator"
  args:
    mesh_pose: [0.0, 0.0, 0.0, 0.00120209, 0.4993256, 0.04823579] # NOTE: From aligning NeRF with world
